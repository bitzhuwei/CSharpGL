using System;
using bitzhuwei.Compiler;

namespace bitzhuwei.PreDirectiveFormat {
    partial class CompilerPreDirective {

#if noTableDFA
        /// <summary>
        /// lexicalState22
        /// <para>CompilerPreDirective.Lexical●[3 NFA States]</para>
        /// </summary>
        private static readonly Action<LexicalContext, char, CurrentStateWrap> lexicalState22 =
        static (context, c, wrap) => {
            if (false) { /* for simpler code generation purpose. */ }
            /* user-input condition code */
            /* = */
            else if (/* possible Vt : '|=' */
            /* no possible signal */
            /* single char */
            c == '='/*'\u003D'(61)*/) {
                ExtendToken(context, st.@Pipe符Equal符);
                wrap.currentState = lexicalState44;
            }
            /* user-input condition code */
            /* \| */
            else if (/* possible Vt : '||' */
            /* no possible signal */
            /* single char */
            c == '|'/*'\u007C'(124)*/) {
                ExtendToken(context, st.@Pipe符Pipe符);
                wrap.currentState = lexicalState45;
            }
            /* deal with everything else. */
            else {
                AcceptToken(st.@Pipe符, context);
                wrap.currentState = lexicalState0;
            }


        };
#endif

    }
}

