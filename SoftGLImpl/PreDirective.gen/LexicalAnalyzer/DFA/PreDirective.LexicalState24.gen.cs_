using System;
using bitzhuwei.Compiler;

namespace bitzhuwei.PreDirectiveFormat {
    partial class CompilerPreDirective {

#if noTableDFA
        /// <summary>
        /// lexicalState24
        /// <para>CompilerPreDirective.Lexical●[3 NFA States]</para>
        /// </summary>
        private static readonly Action<LexicalContext, char, CurrentStateWrap> lexicalState24 =
        static (context, c, wrap) => {
            if (false) { /* for simpler code generation purpose. */ }
            /* user-input condition code */
            /* = */
            else if (/* possible Vt : '&=' */
            /* no possible signal */
            /* single char */
            c == '='/*'\u003D'(61)*/) {
                ExtendToken(context, st.@And符Equal符);
                wrap.currentState = lexicalState48;
            }
            /* user-input condition code */
            /* & */
            else if (/* possible Vt : '&&' */
            /* no possible signal */
            /* single char */
            c == '&'/*'\u0026'(38)*/) {
                ExtendToken(context, st.@And符And符);
                wrap.currentState = lexicalState49;
            }
            /* deal with everything else. */
            else {
                AcceptToken(st.@And符, context);
                wrap.currentState = lexicalState0;
            }


        };
#endif

    }
}

