using System;
using bitzhuwei.Compiler;

namespace bitzhuwei.GLSLFormat {
    partial class CompilerGLSL {

#if noTableDFA
        /// <summary>
        /// lexicalState34
        /// <para>CompilerGLSL.Lexical●[1 DFA States]</para>
        /// </summary>
        private static readonly Action<LexicalContext, char, CurrentStateWrap> lexicalState34 =
        static (context, c, wrap) => {
            if (false) { /* for simpler code generation purpose. */ }
            /* user-input condition code */
            /* = */
            else if (/* possible Vt : '&=' */
            /* no possible signal */
            /* single char */
            c == '='/*'\u003D'(61)*/) {
                ExtendToken(context, st.@And符Equal符);
                wrap.currentState = lexicalState72;
            }
            /* user-input condition code */
            /* & */
            else if (/* possible Vt : '&&' */
            /* no possible signal */
            /* single char */
            c == '&'/*'\u0026'(38)*/) {
                ExtendToken(context, st.@And符And符);
                wrap.currentState = lexicalState73;
            }
            /* deal with everything else. */
            else {
                AcceptToken(st.@And符, context);
                wrap.currentState = lexicalState0;
            }


        };
#endif

    }
}

