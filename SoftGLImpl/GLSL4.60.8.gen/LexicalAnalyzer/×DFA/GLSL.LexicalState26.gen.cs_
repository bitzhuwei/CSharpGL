using System;
using bitzhuwei.Compiler;

namespace bitzhuwei.GLSLFormat {
    partial class CompilerGLSL {

#if noTableDFA
        /// <summary>
        /// lexicalState26
        /// <para>CompilerGLSL.Lexical●[3 NFA States]</para>
        /// </summary>
        private static readonly Action<LexicalContext, char, CurrentStateWrap> lexicalState26 =
        static (context, c, wrap) => {
            if (false) { /* for simpler code generation purpose. */ }
            /* user-input condition code */
            /* = */
            else if (/* possible Vt : '^=' */
            /* no possible signal */
            /* single char */
            c == '='/*'\u003D'(61)*/) {
                ExtendToken(context, st.@Caret符Equal符);
                wrap.currentState = lexicalState64;
            }
            /* user-input condition code */
            /* ^ */
            else if (/* possible Vt : '^^' */
            /* no possible signal */
            /* single char */
            c == '^'/*'\u005E'(94)*/) {
                ExtendToken(context, st.@Caret符Caret符);
                wrap.currentState = lexicalState65;
            }
            /* deal with everything else. */
            else {
                AcceptToken(st.@Caret符, context);
                wrap.currentState = lexicalState0;
            }


        };
#endif

    }
}

